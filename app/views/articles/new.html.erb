<%= form_with model: @article, local: true do |form| %>
  <div class="form-group mb-3">
    <%= form.label :title, "Título", class: 'form-label' %>
    <%= form.text_field :title, class: 'form-control', required: true %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :author, "Autor", class: 'form-label' %>
    <%= form.text_field :author, class: 'form-control', required: true %>
  </div>

  <div class="form-group mb-4">
    <%= form.label :body, "Texto", class: 'form-label' %>
    <%= form.text_area :body, class: 'form-control', required: true, rows: 10 %>
  </div>

  <%= form.submit 'Atualizar Artigo', class: 'btn btn-transparent w-100', data: {disable_with: "Atualizando..."} %>
<% end %>
