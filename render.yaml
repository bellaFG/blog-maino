version: 1
services:
  - type: web
    name: blog-maino
    buildCommand: ./bin/render-build.sh && bundle exec rails db:migrate
    startCommand: bundle exec puma -C config/puma.rb
    env:
      DATABASE_URL: "postgresql://postgres:2579@localhost/blog_db"
